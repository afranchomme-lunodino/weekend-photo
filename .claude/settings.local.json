{
  "permissions": {
    "allow": [
      "Bash(npm install:*)",
      "Bash(cmd.exe /c \"node --version && npm --version\")",
      "Bash(powershell.exe -Command \"node --version; npm --version\")",
      "Bash(npm run *)"
    ]
  }
}
